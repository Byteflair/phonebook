apply plugin: 'groovy'

sourceCompatibility = 1.5
version = '0.1'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
    compile localGroovy()
}